# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
- task: TerraformTaskV2@2
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'MPC-Sandbox-MPN-AH (de9faefb-476e-489b-a289-ab367bcec768)'
    backendAzureRmResourceGroupName: 'AzureDevOpsPOC'
    backendAzureRmStorageAccountName: 'devopsvmterraformstroage'
    backendAzureRmContainerName: 'vmterraformstate'
    backendAzureRmKey: 'terraform.tfstate'

- task: TerraformTaskV2@2
  inputs:
    provider: 'azurerm'
    command: 'plan'
    environmentServiceNameAzureRM: 'MPC-Sandbox-MPN-AH (de9faefb-476e-489b-a289-ab367bcec768)'